import { MassInUnitPipe } from './mass-in-unit.pipe';

describe('MassInUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new MassInUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
