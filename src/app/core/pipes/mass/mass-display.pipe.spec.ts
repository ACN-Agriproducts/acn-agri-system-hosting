import { MassDisplayPipe } from './mass-display.pipe';

describe('MassDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new MassDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
