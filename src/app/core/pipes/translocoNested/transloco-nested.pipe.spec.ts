import { TranslocoNestedPipe } from './transloco-nested.pipe';

describe('TranslocoNestedPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslocoNestedPipe();
    expect(pipe).toBeTruthy();
  });
});
