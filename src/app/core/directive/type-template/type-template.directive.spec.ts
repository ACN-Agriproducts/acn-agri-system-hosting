import { TypeTemplateDirective } from './type-template.directive';

describe('TypeTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
