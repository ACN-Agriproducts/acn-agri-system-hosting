import { ScaleToFitDirective } from './scale-to-fit.directive';

describe('ScaleToFitDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleToFitDirective();
    expect(directive).toBeTruthy();
  });
});
