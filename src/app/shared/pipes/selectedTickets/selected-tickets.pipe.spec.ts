import { SelectedTicketsPipe } from './selected-tickets.pipe';

describe('SelectedTicketsPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedTicketsPipe();
    expect(pipe).toBeTruthy();
  });
});
