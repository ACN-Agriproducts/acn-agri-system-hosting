import { LocalInventoryPipe } from './local-inventory.pipe';

describe('LocalInventoryPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalInventoryPipe();
    expect(pipe).toBeTruthy();
  });
});
