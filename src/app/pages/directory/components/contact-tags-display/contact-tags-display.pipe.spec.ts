import { ContactTagsDisplayPipe } from './contact-tags-display.pipe';

describe('ContactTagsDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactTagsDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
