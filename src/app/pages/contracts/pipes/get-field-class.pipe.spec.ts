import { GetFieldClassPipe } from './get-field-class.pipe';

describe('GetFieldClassPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFieldClassPipe();
    expect(pipe).toBeTruthy();
  });
});
