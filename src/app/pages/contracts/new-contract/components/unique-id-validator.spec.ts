import { UniqueIdValidator } from './unique-id-validator';

describe('UniqueIdValidator', () => {
  it('should create an instance', () => {
    expect(new UniqueIdValidator()).toBeTruthy();
  });
});
