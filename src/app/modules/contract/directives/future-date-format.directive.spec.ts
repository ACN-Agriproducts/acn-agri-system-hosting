import { FutureDateFormatDirective } from './future-date-format.directive';

describe('FutureDateFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new FutureDateFormatDirective();
    expect(directive).toBeTruthy();
  });
});
